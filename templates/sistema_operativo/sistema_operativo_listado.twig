{# SISTEMA OPERATIVO LISTADO#}

{% extends "layout.twig" %}

{% block contenido %}

<legend>Listado de Gerencias</legend>

{% include 'mensaje_alerta.twig' %}

<a class="btn btn-primary" href=" {{ path('sistemaOperativoNuevo') }}">Crea nueva Gerencia</a><br /><br />

<table class="table table-striped table-hover "> 
<thead>
    <tr>
        <th>Item</th>
        <th>Nombre del Sistema Operativo</th>
        <th>Acción</th>
    </tr>
</thead>
<tbody>
     
{% for registro in sistemaOperativos %}
    
<tr>
    <td>{{ loop.index + (5 * pagina) }}</td>
    <td>{{ registro.nombre }}</td>
    <td>	
        <a href="{{ app.url_generator.generate('sistemaOperativoBuscar', {'id': registro.id}) }}">
        <button type="button" class="btn btn-default btn-xs">
        <span class="glyphicon glyphicon-edit"></span>Modificar</button>
        </a>
        <a href="{{ app.url_generator.generate('sistemaOperativoEliminar', {'id': registro.id}) }}" onclick="return confirm_delete();"> 
        <button type="button" class="btn btn-default btn-xs">
        <span class="glyphicon glyphicon-trash"></span>Eliminar</button>
        </a>
    </td>
</tr>

{% endfor %}

</tbody>
</table>

<p class="text-right">Total de Sistemas Operativos: {{numeroRegistro}}</p>

{% if numeroPaginas > 0 %}
    {% include 'sistema_operativo/sistema_operativo_listado_paginador.twig' %}
{% endif %}

{% endblock %}
